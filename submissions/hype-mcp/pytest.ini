[pytest]
asyncio_mode = auto
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -s
# -s flag disables output capture, so print statements are visible
# Use -s to see all print/debug output during tests

